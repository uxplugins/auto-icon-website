import React from "react";
import { Notification } from "rsuite";
export const CopyNotification = (
  <Notification type="success" duration={1500} header="success" closable>
    Copied to clipboard!
  </Notification>
);
